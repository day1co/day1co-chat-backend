{
  "name": "@day1co/cereal-config",
  "version": "0.1.0",
  "description": "configurations by spring cloud config client",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/day1co/cereal.git",
    "directory": "config"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "echo 'no build specified'",
    "clean": "rimraf ./lib ./node_modules ./package-lock.json",
    "clean:build": "rimraf ./lib",
    "lint": "echo 'no lint specified'",
    "prebuild": "npm-run-all clean:build lint",
    "watch": "echo 'no lint specified'",
    "test": "echo 'no test specified'"
  },
  "dependencies": {
    "@day1co/spring-cloud-config-client": "^0.0.9"
  },
  "author": "Day1Company"
}
